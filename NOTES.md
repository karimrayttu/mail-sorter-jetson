# Notes

This repo is organized around the way I tested the sorter:

- motor and encoder code first
- breakbeam/conveyor timing next
- YOLO receiver detection by itself
- OCR and city verification by itself
- final routing after the pieces were stable

The final rotating bin controller is `src/hardware/rotbin_encoder.py`.
